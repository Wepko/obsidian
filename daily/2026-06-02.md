<!-- Empty State - No Orders -->  
       <div class="preview-empty">  
           <div class="preview-empty__content">  
               <h1 class="preview-empty__title">Заказов нет</h1>  
               <p class="preview-empty__description">Тут будут отображаться ваши заказы</p>  
           </div>           <a href="/shop/katalog-tovarov" class="btn btn--filled btn--lg preview-empty__btn">  
               <span>Перейти в каталог</span>  
           </a>       </div>




```php
<?php  
/**  
 * Страничный скрипт для отображения заказа покупателю */defined('_ASTEXE_') or die('No access');  
  
//Для работы с пользователем  
require_once($_SERVER["DOCUMENT_ROOT"] . "/content/users/dp_user.php");  
$user_id = DP_User::getUserId();  
  
//Общая информация по заказам  
require_once($_SERVER["DOCUMENT_ROOT"] . "/content/shop/order_process/orders_background.php");  
  
if (!empty($_POST["action"])) {  
    //Защита от CSRF-атак  
    require_once($_SERVER["DOCUMENT_ROOT"] . "/content/users/stop_csrf.php");  
  
    $error_message = "Ошибка: <br/> Неизвестная операция.";  
    $success_message = "";  
  
    if ($_POST["action"] === 'pay_on_place') {  
        $order_id = (int) $_POST['order_id'];  
  
        $shop_orders_paid_type = array();  
        $query = $db_link->prepare('SELECT * FROM `shop_orders_paid_type` WHERE `active` = 1 ORDER BY `order`;');  
        $query->execute();  
        while ($rov = $query->fetch()) {  
            $shop_orders_paid_type[$rov['id']] = $rov['name'];  
        }  
  
        if ($user_id > 0) {  
            $order_query = $db_link->prepare('SELECT * FROM `shop_orders` WHERE `id` = ? AND `user_id` = ? AND `paid_type` = 0;');  
            $order_query->execute(array($order_id, $user_id));  
            $order = $order_query->fetch();  
            if ($offices_list[$order["office_id"]] !== NULL) {  
                if ($db_link->prepare("UPDATE `shop_orders` SET `paid_type` = 1 WHERE `id` = ?;")->execute(array($order_id)) != true) {  
                    $error_message = "Ошибка: <br/> Не удалось изменить данные.";  
                } else {  
                    $success_message = "Данные сохранены";  
                    $error_message = "";  
                    $db_link->prepare('INSERT INTO `shop_orders_logs` (`order_id`,`time`,`user_id`,`is_manager`,`text`, `is_robot`) VALUES (?, ?, ?, ?, ?, ?);')->execute(array($order_id, time(), $user_id, 0, 'Способ оплаты: <b>' . $shop_orders_paid_type[1] . '</b>', 0));  
  
                    $for_paid_status_query = $db_link->prepare('SELECT `id` FROM `shop_orders_statuses_ref` WHERE `for_paid` = 1;');  
                    $for_paid_status_query->execute();  
                    $for_paid_status_record = $for_paid_status_query->fetch();  
                    if ($for_paid_status_record != false) {  
                        $for_paid_status = $for_paid_status_record["id"];  
                        $username = $DP_Config->http_login;  
                        $password = $DP_Config->http_password;  
                        $context = stream_context_create(array(  
                            'http' => array(  
                                'header'  => "Authorization: Basic " . base64_encode("$username:$password")  
                            )  
                        ));  
                        $set_order_status_result = file_get_contents($DP_Config->domain_path . "content/shop/protocol/set_order_status.php?initiator=4&orders=[$order_id]&status=$for_paid_status&key=" . urlencode($DP_Config->tech_key), false, $context);  
                        $set_order_status_result = json_decode($set_order_status_result, true);  
                        if ($set_order_status_result['status'] == false) {  
                            $error_message = "Предупреждение: <br/> Не удалось сменить статус заказа, сообщите менеджеру для ручной смены статуса.";  
                        }  
                    }  
                }  
            }  
        }  
    }  
    $location_url = '/shop/orders/order?order_id=' . $order_id;  
    ?>  
    <script>location = "<?= $location_url ?>&error_message=<?= $error_message; ?>&success_message=<?= $success_message; ?>";</script>  
    <?php  
    exit;  
}  
  
//Для работы с пользователем  
require_once($_SERVER['DOCUMENT_ROOT'] . "/content/users/dp_user.php");  
$user_session = DP_User::getUserSession();  
  
if ($user_id > 0) {  
    require_once($_SERVER["DOCUMENT_ROOT"] . "/content/general/actions_alert.php");  
  
    $order_id = (int) $_GET["order_id"];  
  
    $WHERE_statuses_not_count = "";  
    for ($i = 0; $i < count($orders_items_statuses_not_count); $i++) {  
        $WHERE_statuses_not_count .= " AND `status` != " . (int)$orders_items_statuses_not_count[$i];  
    }  
  
    $INCOME_SQL = "IFNULL((SELECT SUM(`amount`) FROM `shop_users_accounting` WHERE `active` = 1 AND `income` = 1 AND `order_id` = ?), 0)";  
    $ISSUE_SQL = "IFNULL((SELECT SUM(`amount`) FROM `shop_users_accounting` WHERE `active` = 1 AND `income` = 0 AND `order_id` = ?),0)";  
  
    $order_query = $db_link->prepare("SELECT *, CAST( ($ISSUE_SQL - $INCOME_SQL) AS DECIMAL(20,2) ) AS `paid_sum`, CAST( ( (SELECT SUM(`price`*`count_need`) FROM `shop_orders_items` WHERE `order_id`= `shop_orders`.`id` $WHERE_statuses_not_count ) - ($ISSUE_SQL - $INCOME_SQL) ) AS DECIMAL(20,2) )  AS `paid_left` FROM `shop_orders` WHERE `id` = ? AND `user_id` = ?;");  
    $order_query->execute(array($order_id, $order_id, $order_id, $order_id, $order_id, $user_id));  
    $order = $order_query->fetch();  
    if ($offices_list[$order["office_id"]] == NULL) {  
        echo ("Заказ не найден");  
    } else {  
        $shop_orders_paid_type = array();  
        $query = $db_link->prepare('SELECT * FROM `shop_orders_paid_type` WHERE `active` = 1 ORDER BY `order`;');  
        $query->execute();  
        while ($rov = $query->fetch()) {  
            $shop_orders_paid_type[$rov['id']] = $rov['name'];  
        }  
  
        $time = $order["time"];  
        $office_id = $order["office_id"];  
        $status = $order["status"];  
        $paid = $order["paid"];  
        $paid_type = $order["paid_type"];  
        $paid_sum = $order["paid_sum"];  
        $paid_left = $order["paid_left"];  
        $customer_id = $order["user_id"];  
        $how_get = $order["how_get"];  
        $how_get_json = json_decode($order["how_get_json"], true);  
  
        $db_link->prepare("UPDATE `shop_orders_messages` SET `read` = 1 WHERE `order_id` = ? AND `is_customer` = 0;")->execute(array($order_id));  
        ?>  
  
        <div class="orders-section">  
            <div class="container">  
                <!-- Информационный баннер -->  
                <div class="orders-info-banner" style="margin-bottom: 16px;">  
                    <div class="orders-info-banner__icon">  
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none">  
                            <path d="M12 8V12L15 15M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z" stroke="#F5353C" stroke-width="2" stroke-linecap="round"/>  
                        </svg>                    </div>                    <p class="orders-info-banner__text">  
                        Заказ №<?= $order_id ?> от <?php echo date("d.m.Y", $time) ?>  
                    </p>  
                </div>  
                <!-- Фильтры (информация о заказе) -->  
                    <div class="filters-panel__inner">  
                        <div class="filters-panel__header">  
                            <h1 class="filters-panel__title">Информация о заказе</h1>  
                        </div>                        <div class="filters-panel__dropdowns">  
                            <div class="filter-select" data-select data-field="status">  
                                <div class="filter-select__trigger">  
                                    <span class="filter-select__label">Статус заказа</span>  
                                    <span class="filter-select__arrow">  
                                        <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                    </span>                                </div>                                <div class="filter-select__dropdown">  
                                    <div class="filter-select__options">  
                                        <div class="filter-select__option is-selected" data-value="1"><?php echo $orders_statuses[$status]["name"]; ?></div>  
                                    </div>                                </div>                            </div>                            <div class="filter-select" data-select data-field="paid_type">  
                                <div class="filter-select__trigger">  
                                    <span class="filter-select__label">Способ оплаты</span>  
                                    <span class="filter-select__arrow">  
                                        <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                    </span>                                </div>                                <div class="filter-select__dropdown">  
                                    <div class="filter-select__options">  
                                        <div class="filter-select__option is-selected" data-value="1"><?= (!empty($shop_orders_paid_type[$paid_type])) ? $shop_orders_paid_type[$paid_type] : 'Не выбран'; ?></div>  
                                    </div>                                </div>                            </div>                            <div class="filter-select" data-select data-field="date">  
                                <div class="filter-select__trigger">  
                                    <span class="filter-select__label">Дата создания</span>  
                                    <span class="filter-select__arrow">  
                                        <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                    </span>                                </div>                                <div class="filter-select__dropdown">  
                                    <div class="filter-select__options">  
                                        <div class="filter-select__option is-selected" data-value="1"><?php echo date("d.m.Y", $time) . " " . date("G:i", $time); ?></div>  
                                    </div>                                </div>                            </div>                            <div class="filter-select" data-select data-field="office">  
                                <div class="filter-select__trigger">  
                                    <span class="filter-select__label">Офис</span>  
                                    <span class="filter-select__arrow">  
                                        <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                    </span>                                </div>                                <div class="filter-select__dropdown">  
                                    <div class="filter-select__options">  
                                        <div class="filter-select__option is-selected" data-value="1"><?php echo $offices_list[$office_id]; ?></div>  
                                    </div>                                </div>                            </div>                        </div>                    </div>  
                <!-- Таблица товарных позиций -->  
                <div class="orders-table" style="margin-top: 16px;">  
                    <div class="orders-table__header">  
                        <h2 class="orders-table__title">Товарные позиции</h2>  
                    </div>                    <div class="orders-table__head">  
                        <div class="orders-table__row orders-table__row--header">  
                            <div class="orders-table__cell"><input type="checkbox" id="check_uncheck_all" onchange="on_check_uncheck_all();" /></div>  
                            <div class="orders-table__cell">ID</div>  
                            <div class="orders-table__cell">Производитель</div>  
                            <div class="orders-table__cell">Артикул</div>  
                            <div class="orders-table__cell">Наименование</div>  
                            <div class="orders-table__cell">Цена</div>  
                            <div class="orders-table__cell">Кол-во</div>  
                            <div class="orders-table__cell">Сумма</div>  
                            <div class="orders-table__cell">Срок</div>  
                            <div class="orders-table__cell">Статус</div>  
                        </div>                    </div>                    <div class="orders-table__body">  
                        <?php  
                        $count_need_total = 0;  
                        $price_sum_total = 0;  
  
                        $SELECT_ORDER_ITEMS = "SELECT *, `t2_name` AS `product_name`, `price`*`count_need` AS `price_sum`, `t2_article` AS `article`, `t2_manufacturer` AS `manufacturer` FROM `shop_orders_items` WHERE `order_id` = ?;";  
  
                        $order_items_query = $db_link->prepare($SELECT_ORDER_ITEMS);  
                        $order_items_query->execute(array($order_id));  
  
                        $for_js = "var elements_array = new Array();\n";  
                        $for_js = $for_js . "var elements_id_array = new Array();\n";  
  
                        while ($order_item = $order_items_query->fetch()) {  
                            $item_id = $order_item["id"];  
                            $item_status = $order_item["status"];  
                            $item_count_need = $order_item["count_need"];  
                            $item_price = $order_item["price"];  
                            $item_price_sum = $order_item["price_sum"];  
                            $item_product_name = $order_item["product_name"];  
                            $item_product_manufacturer = $order_item["manufacturer"];  
                            $item_product_article = $order_item["article"];  
                            $item_t2_time_to_exe = $order_item["t2_time_to_exe"];  
                            $item_t2_time_to_exe_guaranteed = $order_item["t2_time_to_exe_guaranteed"];  
  
                            if ($item_t2_time_to_exe < $item_t2_time_to_exe_guaranteed) {  
                                $item_t2_time_to_exe = $item_t2_time_to_exe . " - " . $item_t2_time_to_exe_guaranteed;  
                            }  
                            $item_t2_time_to_exe = $item_t2_time_to_exe . " дн.";  
  
                            $for_js = $for_js . "elements_array[elements_array.length] = \"checked_" . $item_id . "\";\n";  
                            $for_js = $for_js . "elements_id_array[elements_id_array.length] = " . $item_id . ";\n";  
  
                            if (array_search($item_status, $orders_items_statuses_not_count) === false) {  
                                $count_need_total += $item_count_need;  
                                $price_sum_total += $item_price_sum;  
                            }  
                            ?>  
                            <div class="orders-table__row" style="background:<?php echo $orders_items_statuses[$item_status]["color"]; ?>">  
                                <div class="orders-table__cell"><input type="checkbox" onchange="on_one_check_changed('checked_<?php echo $item_id; ?>');" id="checked_<?php echo $item_id; ?>" /></div>  
                                <div class="orders-table__cell"><?php echo $item_id; ?></div>  
                                <div class="orders-table__cell"><?php echo $item_product_manufacturer; ?></div>  
                                <div class="orders-table__cell"><?php echo $item_product_article; ?></div>  
                                <div class="orders-table__cell orders-table__cell--name"><?php echo $item_product_name; ?></div>  
                                <div class="orders-table__cell"><?php echo number_format($item_price, 2, '.', ' '); ?></div>  
                                <div class="orders-table__cell orders-table__cell--center"><?php echo $item_count_need; ?></div>  
                                <div class="orders-table__cell"><?php echo number_format($item_price_sum, 2, '.', ' '); ?></div>  
                                <div class="orders-table__cell"><?php echo $item_t2_time_to_exe; ?></div>  
                                <div class="orders-table__cell">  
                                    <span class="orders-status orders-status--processing"><?php echo $orders_items_statuses[$item_status]["name"]; ?></span>  
                                </div>                            </div>                        <?php } ?>  
                        <div class="orders-table__row orders-table__row--total">  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell orders-table__cell--total-label">Итого:</div>  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell orders-table__cell--center"><strong><?php echo $count_need_total; ?></strong></div>  
                            <div class="orders-table__cell"><strong><?php echo number_format($price_sum_total, 2, '.', ' '); ?></strong></div>  
                            <div class="orders-table__cell"></div>  
                            <div class="orders-table__cell"></div>  
                        </div>                    </div>                </div>  
                <!-- Кнопки действий с товарами -->  
                <div class="orders-filters" style="margin-top: 16px;">  
                    <div class="orders-filters__actions">  
                        <?php if ($DP_Config->return_available == '1') : ?>  
                            <button onclick="confirm_return();" class="btn btn--outline btn--sm">  
                                <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none"><path d="M3 10H13C17.4183 10 21 13.5817 21 18C21 18.5 20.5 18.5 20 18.5C19 18.5 18 18.5 17 18.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><path d="M7 14L3 10L7 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>  
                                Оформить возврат  
                            </button>  
                        <?php endif; ?>  
                        <button onclick="cancel_positions();" class="btn btn--outline btn--sm">  
                            <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none"><path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>  
                            Отменить позиции  
                        </button>  
                    </div>                </div>  
                <!-- Платежи по заказу -->  
                <div class="orders-table" style="margin-top: 16px;">  
                    <div class="orders-table__header">  
                        <h2 class="orders-table__title">Платежи по заказу</h2>  
                    </div>                    <div class="orders-table__body">  
                        <div class="orders-table__row">  
                            <div class="orders-table__cell"><strong>Состояние оплаты:</strong></div>  
                            <div class="orders-table__cell">  
                                <span class="orders-status <?php echo ($paid == 0) ? 'orders-status--pending' : (($paid == 1) ? 'orders-status--delivered' : 'orders-status--processing'); ?>">  
                                    <?php  
                                    switch ($paid) {  
                                        case 0: echo 'Не оплачен'; break;  
                                        case 1: echo 'Оплачен полностью'; break;  
                                        case 2: echo 'Оплачен частично'; break;  
                                    }  
                                    ?>  
                                </span>  
                            </div>                            <div class="orders-table__cell"><strong>Сумма заказа:</strong></div>  
                            <div class="orders-table__cell"><?php echo $price_sum_total; ?> ₽</div>  
                            <div class="orders-table__cell"><strong>Оплачено:</strong></div>  
                            <div class="orders-table__cell"><?php echo $paid_sum; ?> ₽</div>  
                            <div class="orders-table__cell"><strong>Осталось оплатить:</strong></div>  
                            <div class="orders-table__cell"><?php echo $paid_left; ?> ₽</div>  
                        </div>                    </div>                </div>  
                <!-- Форма оплаты -->  
                <?php if ($paid != 1) {  
                    $office_SQL = "";  
                    $balance_binging_values = array($user_id, $user_id, $user_id);  
                    if (isset($DP_Config->wholesaler)) {  
                        $office_SQL = " AND `office_id` = ? ";  
                        $balance_binging_values = array($user_id, $office_id, $user_id, $office_id, $user_id);  
                    }  
                    $INCOME_SQL = "IFNULL((SELECT SUM(`amount`) FROM `shop_users_accounting` WHERE `user_id` = ? AND `income`=1 AND `active` = 1 " . $office_SQL . "), 0)";  
                    $ISSUE_SQL = "IFNULL((SELECT SUM(`amount`) FROM `shop_users_accounting` WHERE `user_id` = ? AND `income`=0 AND `active` = 1 " . $office_SQL . "),0)";  
                    $balance_query = $db_link->prepare("SELECT *, CAST( ($INCOME_SQL-$ISSUE_SQL) AS DECIMAL(20,2) ) AS `balance` FROM `shop_users_accounting` WHERE `user_id` = ?;");  
                    $balance_query->execute($balance_binging_values);  
                    $balance_record = $balance_query->fetch();  
                    $balance = $balance_record["balance"];  
                    if ($balance == "") $balance = 0;  
  
                    $balance_pay_available = false;  
                    $sum_for_check_balance = $paid_left;  
                    if ($DP_Config->partial_payment) {  
                        $min_pay = $price_sum_total * ($DP_Config->partial_payment_min_percent / 100);  
                        $sum_for_check_balance = $min_pay;  
                    }  
                    $balance_pay_limit = 0;  
  
                    if ($balance >= $sum_for_check_balance) {  
                        $balance_pay_available = true;  
                        if (!$DP_Config->partial_payment) {  
                            $balance_pay_limit = $paid_left;  
                        } else {  
                            if ($balance >= $paid_left) {  
                                $balance_pay_limit = $paid_left;  
                            } else {  
                                if ($DP_Config->client_overdraft) {  
                                    if ((int)$DP_Config->client_overdraft_value == 0) {  
                                        $balance_pay_limit = $paid_left;  
                                    } else {  
                                        $balance_pay_limit = $balance + (int)$DP_Config->client_overdraft_value;  
                                    }  
                                } else {  
                                    $balance_pay_limit = $balance;  
                                }  
                            }  
                        }  
                    } else {  
                        if ($DP_Config->client_overdraft) {  
                            if ((int)$DP_Config->client_overdraft_value == 0) {  
                                $balance_pay_available = true;  
                                $balance_pay_limit = $paid_left;  
                            } else if ($sum_for_check_balance - $balance <= (int)$DP_Config->client_overdraft_value) {  
                                $balance_pay_available = true;  
                                $balance_pay_limit = (int)$DP_Config->client_overdraft_value + $balance;  
                            }  
                        }  
                    }  
                    if ($balance_pay_limit == 0) $balance_pay_available = false;  
                    ?>  
                        <div class="filters-panel__inner">  
                            <div class="filters-panel__header">  
                                <h3 class="filters-panel__title" style="font-size: 20px;">Оплата заказа</h3>  
                            </div>                            <div class="filters-panel__dropdowns">  
                                <?php if ($DP_Config->partial_payment) { ?>  
                                    <div class="filter-select" data-select data-field="pay_source">  
                                        <div class="filter-select__trigger">  
                                            <span class="filter-select__label">Способ оплаты</span>  
                                            <span class="filter-select__arrow">  
                                                <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                            </span>                                        </div>                                        <div class="filter-select__dropdown">  
                                            <div class="filter-select__options">  
                                                <div class="filter-select__option is-selected" data-value="1">Платеж через сайт</div>  
                                                <?php if ($balance_pay_available) { ?>  
                                                    <div class="filter-select__option" data-value="0">Оплата с баланса (доступно <?php echo $balance_pay_limit; ?> ₽)</div>  
                                                <?php } ?>  
                                            </div>  
                                        </div>                                    </div>                                    <div class="filter-select" data-select data-field="amount">  
                                        <div class="filter-select__trigger">  
                                            <span class="filter-select__label">Сумма платежа</span>  
                                            <span class="filter-select__arrow">  
                                                <svg class="icon icon--sm" viewBox="0 0 24 24"><use href="#icon-chevron-down"></use></svg>  
                                            </span>                                        </div>                                        <div class="filter-select__dropdown">  
                                            <div class="filter-select__options">  
                                                <?php if ($paid_left <= $min_pay) { ?>  
                                                    <div class="filter-select__option is-selected" data-value="<?php echo $paid_left; ?>"><?php echo $paid_left; ?> ₽</div>  
                                                <?php } else { ?>  
                                                    <div class="filter-select__option" data-value="custom">Своя сумма</div>  
                                                <?php } ?>  
                                            </div>  
                                        </div>                                    </div>                                    <div class="filter-select--custom" style="display: <?php echo ($paid_left > $min_pay) ? 'block' : 'none'; ?>;">  
                                        <input type="number" class="orders-filters__input" id="pay_value" placeholder="Введите сумму" value="<?php echo $paid_left; ?>" style="width: 100%; padding: 12px 16px; border-radius: 12px; border: 1px solid #dcdbe0;">  
                                    </div>                                <?php } else { ?>  
                                    <input type="hidden" id="pay_value" value="<?php echo $paid_left; ?>" />  
                                    <div class="orders-filters__actions">  
                                        <button onclick="add_payment_to_order(1);" class="btn btn--filled">Оплатить online</button>  
                                        <?php if ($balance_pay_available) { ?>  
                                            <button onclick="add_payment_to_order(0);" class="btn btn--outline">Оплатить с баланса</button>  
                                        <?php } ?>  
                                    </div>  
                                <?php } ?>  
  
                                <?php if ($DP_Config->order_pay_on_place == 1 && $paid == false && $paid_type == 0) { ?>  
                                    <div class="orders-filters__actions">  
                                        <button onclick="pay_on_place();" class="btn btn--outline"><?php echo $shop_orders_paid_type[1]; ?></button>  
                                    </div>                                    <form id="pay_form" name="pay_form" method="POST" style="display:none;">  
                                        <input type="hidden" name="action" id="action" value="pay_on_place" />  
                                        <input type="hidden" name="order_id" value="<?= $order_id; ?>" />  
                                        <input type="hidden" name="csrf_guard_key" value="<?php echo $user_session["csrf_guard_key"]; ?>" />  
                                    </form>                                <?php } ?>  
                            </div>  
                            <?php if ($DP_Config->partial_payment && $paid_left > $min_pay) { ?>  
                                <div style="margin-top: 16px;">  
                                    <button onclick="add_payment_to_order(2);" class="btn btn--filled">Оплатить</button>  
                                </div>                            <?php } ?>  
                        </div>  
                <?php } ?>  
  
                <!-- Печать документов -->  
                <?php  
                $print_docs_buttons = "";  
                $print_docs_query = $db_link->prepare("SELECT * FROM `shop_print_docs` ORDER BY `id` ASC;");  
                $print_docs_query->execute();  
                while ($print_doc = $print_docs_query->fetch()) {  
                    $print_doc["parameters_values"] = json_decode($print_doc["parameters_values"], true);  
                    if (isset($DP_Config->wholesaler)) {  
                        $doc_query_2 = $db_link->prepare("SELECT * FROM `shop_print_docs_wholesaler` WHERE `doc_name` = ? AND `office_id` = ( SELECT `office_id` FROM `shop_orders` WHERE `id` = ? ) ;");  
                        $doc_query_2->execute(array($print_doc["name"], $order_id));  
                        $doc_record_2 = $doc_query_2->fetch();  
                        if ($doc_record_2 != false) {  
                            $print_doc["parameters_values"] = json_decode($doc_record_2["parameters_values"], true);  
                        }  
                    }  
                    if ((int)$print_doc["parameters_values"]["button_visible_for_customer"] != 1) continue;  
                    $print_docs_buttons .= "<a class=\"btn btn--outline btn--sm\" href=\"/content/shop/print_docs/service/print.php?doc_name=" . $print_doc["name"] . "&order_id=" . $order_id . "&csrf_guard_key=" . $user_session["csrf_guard_key"] . "\" target=\"_blank\"><i class=\"fa fa-print\"></i> " . $print_doc["caption"] . "</a> ";  
                }  
                if ($print_docs_buttons != "") { ?>  
                    <div class="orders-table" style="margin-top: 16px;">  
                        <div class="orders-table__header">  
                            <h2 class="orders-table__title">Печать документов</h2>  
                        </div>                        <div class="orders-table__body">  
                            <div class="orders-filters__actions"><?php echo $print_docs_buttons; ?></div>  
                        </div>                    </div>                <?php } ?>  
  
                <!-- Способ получения -->  
                <div class="orders-table" style="margin-top: 16px;">  
                    <div class="orders-table__header">  
                        <h2 class="orders-table__title">Способ получения</h2>  
                    </div>                    <div class="orders-table__body">  
                        <?php  
                        $obtain_query = $db_link->prepare('SELECT * FROM `shop_obtaining_modes` WHERE `id` = ?;');  
                        $obtain_query->execute(array($how_get));  
                        $obtain_mode = $obtain_query->fetch();  
                        require_once($_SERVER["DOCUMENT_ROOT"] . "/content/shop/obtaining_modes/" . $obtain_mode["handler"] . "/show_actual_info.php");  
                        ?>  
                    </div>  
                </div>  
                <!-- Переписка с продавцом -->  
                <div class="orders-table" style="margin-top: 16px;">  
                    <div class="orders-table__header">  
                        <h2 class="orders-table__title">Переписка с продавцом</h2>  
                    </div>                    <div class="orders-table__body">  
                        <div class="chat-messages" id="chat_block" style="height: 300px; overflow-y: auto; border: 1px solid #dcdbe0; border-radius: 12px; padding: 16px; margin-bottom: 16px; background: #f4f5f6;"></div>  
                        <div class="chat-input" style="display: flex; gap: 12px;">  
                            <textarea id="new_message_area" class="orders-filters__input" rows="3" placeholder="Введите сообщение..." style="flex: 1; padding: 12px 16px; border-radius: 12px; border: 1px solid #dcdbe0; resize: vertical;"></textarea>  
                            <button class="btn btn--filled" onclick="sendMessage();" style="align-self: flex-end;">Отправить</button>  
                        </div>                    </div>                </div>            </div>        </div>  
        <style>            .orders-table__cell--center { text-align: center; }  
            .orders-table__cell--name { min-width: 200px; max-width: 300px; word-break: break-word; white-space: normal; }  
            .orders-table__cell--total-label { text-align: right; font-weight: 700; }  
            .orders-table__row--total { background: #f4f5f6; font-weight: 700; }  
            .chat-messages .bubble { background: #fff; border-radius: 12px; padding: 10px 15px; margin-bottom: 10px; border: 1px solid #dcdbe0; }  
            .chat-messages .bubble2 { background: #f5353c; color: #fff; border-radius: 12px; padding: 10px 15px; margin-bottom: 10px; }  
            .chat-messages .bubble strong, .chat-messages .bubble2 strong { font-weight: 600; }  
        </style>  
  
        <script>            function getOrderMessages() {  
                jQuery.ajax({  
                    type: "GET", async: true,  
                    url: "/content/shop/messager/ajax_get_order_messages.php",  
                    dataType: "json",  
                    data: "order_id=<?php echo $order_id; ?>" + "&csrf_guard_key=<?php echo $user_session["csrf_guard_key"]; ?>",  
                    success: function(answer) {  
                        var html = "";  
                        for (var i = 0; i < answer.length; i++) {  
                            var class_str = "bubble";  
                            var sender = "Покупатель";  
                            if (answer[i].is_customer == false) {  
                                class_str += "2";  
                                sender = "Продавец";  
                            }  
                            html += "<div class=\"" + class_str + "\"><strong>" + sender + "</strong> " + answer[i].time + "<br>" + answer[i].text + "</div>";  
                        }  
                        if (html == "") html = "<div style=\"text-align:center; padding:40px; color:#858585;\">Сообщений в данном заказе нет</div>";  
                        document.getElementById("chat_block").innerHTML = html;  
                        document.getElementById("chat_block").scrollTop = document.getElementById("chat_block").scrollHeight;  
                    }  
                });  
            }  
  
            function sendMessage() {  
                var text = document.getElementById("new_message_area").value;  
                if (text == "") { alert("Поле для сообщение пустое"); return; }  
                jQuery.ajax({  
                    type: "GET", async: true,  
                    url: "/content/shop/messager/ajax_send_message.php",  
                    dataType: "json",  
                    data: "order_id=<?php echo $order_id; ?>&text=" + encodeURI(text) + "&csrf_guard_key=<?php echo $user_session["csrf_guard_key"]; ?>",  
                    success: function(answer) {  
                        if (answer == true) {  
                            document.getElementById("new_message_area").value = "";  
                            getOrderMessages();  
                        } else { alert("Ошибка отправки сообщения"); }  
                    }  
                });  
            }  
  
            getOrderMessages();  
            setInterval(function() { getOrderMessages(); }, 300000);  
  
            <?php echo $for_js; ?>  
  
            function on_check_uncheck_all() {  
                var state = document.getElementById("check_uncheck_all").checked;  
                for (var i = 0; i < elements_array.length; i++) {  
                    document.getElementById(elements_array[i]).checked = state;  
                }  
            }  
  
            function on_one_check_changed(id) {  
                for (var i = 0; i < elements_array.length; i++) {  
                    if (document.getElementById(elements_array[i]).checked == false) {  
                        document.getElementById("check_uncheck_all").checked = false;  
                        break;  
                    }  
                }  
            }  
  
            function getCheckedElements() {  
                var checked_ids = new Array();  
                for (var i = 0; i < elements_array.length; i++) {  
                    if (document.getElementById(elements_array[i]).checked == true) {  
                        checked_ids.push(elements_id_array[i]);  
                    }  
                }  
                return checked_ids;  
            }  
  
            function confirm_return() {  
                if (getCheckedElements().length < 1) {  
                    alert("Выберите позиции, для которых нужно оформить возврат.");  
                    return false;  
                } else {  
                    let obj = new URLSearchParams({ items_id: getCheckedElements(), csrf_guard_key: '<?php echo $user_session["csrf_guard_key"]; ?>' });  
                    jQuery.ajax({  
                        type: "POST", async: true,  
                        url: "<?php echo $DP_Config->domain_path; ?>content/shop/order_process/ajax_check_items_returns.php",  
                        dataType: "json", data: obj.toString(),  
                        success: function(answer) {  
                            if (answer.count_confirm > 0 || !answer.all_complete) {  
                                alert("На выбранные позиции нельзя оформить возврат.");  
                            } else {  
                                location = "/shop/returns/add_return?items=" + JSON.stringify(getCheckedElements());  
                            }  
                        }  
                    });  
                }  
            }  
  
            function cancel_positions() {  
                if (getCheckedElements().length < 1) {  
                    alert("Выберите позиции, которые хотите отменить.");  
                    return false;  
                } else {  
                    document.getElementById("new_message_area").value = 'Запрос на отмену позиций: ' + getCheckedElements();  
                    sendMessage();  
                    alert("Ваш запрос на отмену отправлен менеджеру.");  
                }  
            }  
  
            function pay_on_place() {  
                if (!confirm('Внимание! Происходит выбор способа оплаты заказа. Продолжить?')) return;  
                document.forms["pay_form"].submit();  
            }  
        </script>  
  
        <?php if ($paid != 1) { ?>  
            <script>  
                function add_payment_to_order(direct_pay) {  
                    var pay_value = document.getElementById('pay_value') ? document.getElementById('pay_value').value : <?php echo $paid_left; ?>;  
                    pay_value = parseFloat(pay_value).toFixed(2);  
                    if (pay_value == '' || pay_value == 'NaN') { alert('Укажите сумму платежа'); return; }  
                    if (pay_value > <?php echo $paid_left; ?> || pay_value <= 0) { alert('Сумма не должна превышать остаток долга по заказу'); return; }  
  
                    <?php if ($DP_Config->partial_payment) { ?>  
                    if (pay_value < <?php echo $paid_left; ?> && pay_value < <?php echo $min_pay; ?>) {  
                        alert('Минимальная сумма платежа: <?php echo $min_pay; ?>');  
                        return;  
                    }  
                    <?php } ?>  
  
                    if (direct_pay == 2) {  
                        var selectedPaySource = document.querySelector('[data-select][data-field="pay_source"] .filter-select__option.is-selected');  
                        direct_pay = selectedPaySource ? selectedPaySource.getAttribute('data-value') : 1;  
                    }  
  
                    if (direct_pay == 0) {  
                        if (pay_value > <?php echo $balance_pay_limit; ?>) {  
                            alert('Доступный лимит при оплате с баланса: <?php echo $balance_pay_limit; ?>');  
                            return;  
                        }  
                        <?php if ($balance < 0) { ?>  
                        if (!confirm('Внимание! На Вашем счете задолженность. Оплатить с баланса?')) return;  
                        <?php } else { ?>  
                        if (pay_value > <?php echo $balance; ?> && !confirm('Внимание! На вашем счету недостаточно средств. Оплатить с баланса?')) return;  
                        <?php } ?>  
                        jQuery.ajax({  
                            type: "GET", async: false,  
                            url: "/content/shop/protocol/pay_for_order.php",  
                            dataType: "text",  
                            data: "initiator=2&order_id=<?php echo $order_id; ?>&direct_pay=" + direct_pay + "&pay_sum=" + pay_value + "&csrf_guard_key=<?php echo $user_session["csrf_guard_key"]; ?>",  
                            success: function(answer) {  
                                var answer_ob = JSON.parse(answer);  
                                if (typeof answer_ob.status === "undefined") alert("Неизвестная ошибка");  
                                else if (answer_ob.status == true) location = '/shop/orders/order?order_id=<?php echo $order_id; ?>&success_message=Платеж успешно добавлен';  
                                else alert(answer_ob.message);  
                            }  
                        });  
                    } else {  
                        var request_object = { order_id: <?php echo $order_id; ?>, amount: pay_value };  
                        jQuery.ajax({  
                            type: "POST", async: false,  
                            url: "/content/shop/finance/ajax_create_operation.php",  
                            dataType: "text",  
                            data: "request_object=" + encodeURI(JSON.stringify(request_object)) + "&csrf_guard_key=<?php echo $user_session["csrf_guard_key"]; ?>",  
                            success: function(answer) {  
                                var answer_ob = JSON.parse(answer);  
                                if (typeof answer_ob.result == 'undefined') alert("Ошибка парсинга результата");  
                                else if (answer_ob.result == true) {  
                                    if (answer_ob.pay_system == 0) alert("К сайту не подключена платежная система");  
                                    else location = "/content/shop/finance/payment_systems/" + answer_ob.pay_system + "/go_to_pay.php?operation=" + answer_ob.operation + "&csrf_guard_key=<?php echo $user_session["csrf_guard_key"]; ?>";  
                                } else alert("Ошибка создания операции");  
                            }  
                        });  
                    }  
                }  
            </script>  
        <?php } ?>  
    <?php }  
} ?>
```