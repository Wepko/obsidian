Твоя задача — сверстать дизайн сдля этого следуй стратегии read_design_strategy для анализа этого Figma-макета  по ссылке https://www.figma.com/design/sAvi4RelFd2RRtVi6feRBl/atw.parts--Copy-Main-?node-id=17406-5231&t=9WhseVkPxZwuoNPX-4

СНАЧАЛА:
1. Вызови get_metadata (node-id из ссылки)
2. Вызови get_design_context с depth=3 (чтобы увидеть ID всех значимых узлов)
3. ДЛЯ КАЖДОГО узла, у которого childCount > 0:
   - Вызови get_node(его ID) — ОБЯЗАТЕЛЬНО!
   - Проанализируй всех детей рекурсивно
4. Только после get_node для всех узлов начинай анализ
НЕ ИСПОЛЬЗУЙ get_design_context как единственный источник данных — он не раскрывает детей!

ВО ВРЕМЯ АНАЛИЗА:
- Определи, какие фреймы имеют Auto Layout
- Определи padding, gap, alignment
- После спроси у меня можешь ли ты делать дальш, и покажи все как увидел и как понял своими словами опишешь что есть на макете 

ТОЛЬКО ПОСЛЕ ЭТОГО:
У нас  есть atw-form.html нужно создать еще одну форму и сделай это по экранам чтоб была секция высотой 100vh и там была моя новая форма Доабвить автомобиль она очень похоже по стилям которое уже есть нужно как-то     их переиспользовать  _atw-form.scss для инпут и select использовать компоненты _filter-select.scss _input.scss 

  переиспользовать тоже самое но с другими заголовками и другими полями  
делай анализ всего проекта atw-custom-html в src нужно сверставть новый экран и scss стили
Перед версткой прочитай руководство в docs/design-analysis-pro.md и начинай верстать 

Никогда не пиши код без выполнения всех MCP-вызовов.