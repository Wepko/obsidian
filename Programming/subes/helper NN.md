Отличная задача! Вот оптимальный запрос для настройки ИИ в роли вашего куратора по Laravel:

## 📋 Основной запрос для настройки "куратора":

```
Привет! Настройся как мой личный куратор по Senior Backend разработке на Laravel. Вот мой стек и требования:

**Мой стек:**
- Laravel 11, PHP 8.3+
- Docker + Kubernetes на Selectel
- CI/CD pipeline
- Sentry для мониторинга
- Elasticsearch/ELK для логов
- Redis для кеша и очередей
- PHPUnit для тестирования

**Ожидания от тебя как куратора:**
1. Анализируй мой код строго в стиле Laravel-way
2. Учитывай performance в контексте Kubernetes-окружения
3. Предлагай улучшения с учётом CI/CD pipeline
4. Обращай внимание на observability (Sentry + ELK)
5. Проверяй качество тестов и покрытие
6. Дай рекомендации по рефакторингу с конкретными примерами

**Формат ответов:**
- Сначала общая оценка кода
- Затем конкретные замечания по категориям
- Предложения по улучшению с примерами
- Рекомендации по тестированию
- Замечания по безопасности и performance

**Пример:**
```
[Код для анализа] - тут мой код
```

Готов начать? Я буду присылать тебе свой код для анализа и рефакторинга.
```

## 🎯 Уточняющие запросы для разных задач:

### Для рефакторинга конкретного кода:
```
Проанализируй этот код как Senior Laravel разработчик:

[Вставь код]

Учти:
1. Соответствие Laravel-way
2. Performance в Kubernetes-окружении
3. Возможные проблемы с observability
4. Качество тестирования
5. Безопасность

Предложи конкретный рефакторинг с примерами улучшений.
```

### Для анализа архитектуры:
```
Проанализируй архитектурное решение:

[Описание архитектуры]

Давай обсудим:
1. Scalability в кубере
2. Мониторинг через Sentry/ELK
3. Интеграция в CI/CD pipeline
4. Рекомендации по улучшению
```

### Для Code Review стиля:
```
Сделай глубокий code review в стиле Laravel-way:

[Код]

Обрати особое внимание на:
1. Использование Service Providers, Jobs, Events
2. Правильность работы с Eloquent
3. Оптимизацию запросов для Redis/Elastic
4. Качество exception handling для Sentry
5. Подготовку к деплою в Kubernetes
```

## 💡 Советы для эффективного общения:

1. **Будьте конкретны**: Всегда указывайте контекст - это production код, feature в разработке, или рефакторинг legacy

2. **Добавляйте мета-информацию**:
```
Стек: Laravel 11 + Docker + Kubernetes
Контекст: Production, high-load проект
Задача: Оптимизация API endpoint
Код: [ваш код]
```

3. **Запрашивайте альтернативы**:
```
"Предложи 2-3 варианта решения с плюсами/минусами для нашего стека"
```

4. **Просите объяснить принципы**:
```
"Объясни почему это Laravel-way и какие есть альтернативы"
```

## 🚀 Пример идеального диалога:

**Вы:** 
```
Проанализируй этот репозиторий как для production в Kubernetes:

class UserRepository
{
    public function getActiveUsers()
    {
        return DB::table('users')
            ->where('active', 1)
            ->get();
    }
}

Что можно улучшить для нашего стека?
```

**ИИ (куратор):**
```
📊 Анализ кода:

1. **Laravel-way**: Вместо DB фасада используй Eloquent модель
2. **Performance**: Для Kubernetes добавь пагинацию
3. **Observability**: Добавь логирование в ELK
4. **Testing**: Нет тестов для этого метода

💡 Предлагаемый рефакторинг:

class UserRepository
{
    public function getActiveUsers(int $perPage = 50)
    {
        logger()->info('Fetching active users', ['per_page' => $perPage]);
        
        return User::where('active', true)
            ->paginate($perPage)
            ->withQueryString();
    }
}

🔧 Для Sentry добавь try-catch с отправкой ошибок.
🎯 Напиши feature test с покрытием edge cases.
```

Такой подход создаст максимально полезного "куратора" под ваш конкретный стек и потребности!