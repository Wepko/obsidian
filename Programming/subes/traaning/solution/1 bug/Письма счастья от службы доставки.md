Отличный запрос. Лови второй баг, который идеально ложится на твой опыт интеграции службы доставки, очередей и Sentry. Здесь не будет deadlock-ов, зато будет **идемпотентность** — тема, которая на проде превращает «мелкую недоработку» в «рассылку спама всем клиентам».

---

## 📦 Баг №2: «Письма счастья от службы доставки»

### Контекст
В проект внедряется **интеграция со службой доставки** (СДЭК / Boxberry / PickPoint).  
По документации: при смене статуса отправления (принят на складе, передан курьеру, вручён) служба присылает **POST-вебхук** на наш API.  

Мы реализовали эндпоинт, который:
1. Принимает вебхук.
2. Валидирует подпись.
3. Диспатчит **Job** (через очередь) для обновления статуса заказа в БД.
4. Внутри Job — изменение статуса заказа, запись в лог, отправка email-уведомления клиенту.

Всё покрыто тестами (PHPUnit), Sentry настроен, ошибки ловятся.  
Деплоим на прод.

### Симптомы
Через пару дней:
- Клиенты звонят в поддержку: **«Мне пришло 15 писем, что заказ “Передан в доставку”!»**
- В Sentry — **тишина**, никаких эксепшенов.
- В очереди Redis — **тысячи одинаковых Job**, висят с задержкой, latency синхронизации упала.
- Периодически всплывают ошибки `Duplicate entry '...' for key 'order_status_logs_unique'`, но Sentry настроен игнорировать duplicate key violations? (или не настроен — зависит).

### Локальное тестирование
Всё идеально:
- Шлём один вебхук через Postman → приходит одно письмо.
- Шлём два одинаковых вебхука с интервалом 5 секунд → второй раз Job видит, что статус уже `shipped`, проверяет `if ($order->status === $newStatus) { return; }` и не отправляет письмо.  
**Но почему на проде так не работает?**

---

## 🔍 Расследование

### Шаг 1. Логи вебхуков
Смотрим таблицу входящих запросов (у нас было логирование тела вебхука).  
Обнаруживаем: **один и тот же заказ получает 3–4 вебхука с одинаковым статусом, но с разными `timestamp` и разными `delivery_event_id`**.  

Оказывается, служба доставки **ретраит вебхуки при таймаутах/ошибках** (а мы отвечаем 200 OK, но иногда из‑за очереди ответ может задержаться, и доставка думает, что мы не приняли).  
Локально мы не эмулировали повторную отправку с интервалом 100 мс — а на проде это происходит постоянно.

### Шаг 2. Гонка в очереди
Смотрим код Job:
```php
public function handle()
{
    $order = Order::find($this->orderId);
    if ($order->status === $this->newStatus) {
        return; // уже обновлено, ничего не делаем
    }
    $order->status = $this->newStatus;
    $order->save();
    Mail::send(...); // отправляем письмо
}
```

**Проблема**: проверка `$order->status === $this->newStatus` **не атомарна**.  
Если два Job для одного заказа выполняются **параллельно** в разных процессах воркеров, возможна ситуация:
- Job A читает статус = `processing`, обновляет на `shipped`, но ещё не закоммитил транзакцию.
- Job B (пришедший через 50 мс) читает старый статус (`processing`) и тоже начинает обновление.
- В итоге оба Job отправляют письма и дважды обновляют статус (второй раз статус уже тот же, но письмо уже ушло).

Локально при последовательном выполнении такого нет, а на проде при 20 воркерах — race condition обеспечен.

### Шаг 3. Почему Sentry молчал?
Ошибки `Duplicate entry` возникали, но в Sentry мы их **целенаправленно фильтровали** (после первого бага с deadlock-ами решили, что дубли ключей — это нормально, бывает повторная отправка формы).  
Ирония: фильтр скрыл реальную проблему идемпотентности.

---

## 🧠 Решение

### 1. Идемпотентность на уровне БД
Добавляем таблицу `delivery_webhooks`:
```sql
CREATE TABLE delivery_webhooks (
    id BIGSERIAL PRIMARY KEY,
    external_event_id VARCHAR(255) NOT NULL UNIQUE,
    order_id BIGINT NOT NULL,
    status VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW()
);
```
Вебхук от доставки всегда содержит `event_uuid` (или свой уникальный идентификатор).  
При обработке:
```php
if (DeliveryWebhook::where('external_event_id', $eventId)->exists()) {
    return; // уже обработали, игнорируем
}
DB::transaction(function () use ($eventId, $order, $newStatus) {
    DeliveryWebhook::create(['external_event_id' => $eventId, 'order_id' => $order->id, 'status' => $newStatus]);
    $order->update(['status' => $newStatus]);
    Mail::send(...);
});
```
**Уникальный индекс** на `external_event_id` гарантирует, что второй параллельный Job вызовет `DuplicateKeyException`, который мы ловим и спокойно игнорируем.

### 2. Оптимистичная блокировка / CAS
Можно обойтись без доп. таблицы, используя **версионирование строк**:
```sql
UPDATE orders SET status = ?, version = version + 1 WHERE id = ? AND version = ?;
```
Если затронуто 0 строк — значит, статус уже обновлён (или изменилась версия).  
Но для нашей истории идемпотентность через таблицу событий — нагляднее.

### 3. Настройка Sentry
Убираем фильтр на `DuplicateKeyException`, наоборот — ловим их, но с низким приоритетом (`warning`), чтобы видеть, сколько дублей прилетает.  
Добавляем теги `delivery_service`, `event_type` — это позволит быстро заметить аномалии.

### 4. Circuit Breaker для доставки
Если служба доставки начинает слать пачками дубли, можно временно блокировать обработку её вебхуков.  
Но в рамках MVP ограничились идемпотентностью.

---

## 💥 Итог
После деплоя:
- Письма перестали дублироваться.
- Очередь перестала забиваться холостыми Job.
- Sentry начал показывать статистику дублей — оказалось, до 15% вебхуков — повторные.
- Клиенты довольны.

---

## 🔗 Как это связано с твоим опытом

✅ **Интеграция службы доставки** — центральная роль в баге.  
✅ **Работа с очередями** — Job, параллельные воркеры, race condition.  
✅ **Sentry** — сначала скрыл проблему из-за фильтра, потом помог её увидеть.  
✅ **Тесты (PHPUnit)** — не покрывали многопоточность, поэтому локально не ловилось.  
✅ **Неочевидность** — думали, что проверки статуса достаточно, но забыли про конкурентность.  
✅ **«Воспроизводится только под нагрузкой»** — нужно много одновременных вебхуков на один заказ, чтобы поймать гонку.  

Этот случай — классика **интеграционной разработки**, когда внешний сервис ведёт себя «чуть умнее», чем мы ожидаем, и на проде это вылезает боком. Ты вполне мог столкнуться с таким при подключении новой доставки к e‑commerce проекту.