Вот конкретная история из практики, которая идеально ложится на твой опыт: **Честный знак, Excel‑отчёты, интеграция с Ozon/WB и очередь задач**. Баг был максимально неочевидным, локально не воспроизводился, а на проде валил ночные кроны.

---

## 🐞 Сценарий: «Мёртвая хватка отчёта и маркетплейса»

### Что делали
Ты добавил в админку **выгрузку Excel‑отчёта по товарам с Честным знаком**.  
Таблицы спроектированы в PostgreSQL, отчёт строит сложный `SELECT` с `JOIN` нескольких таблиц:  
`products`, `honest_sign_codes`, `orders`, `order_items`.  

Параллельно работает **интеграция с Ozon и Wildberries**:  
- Job‑ы обновляют остатки и статусы заказов по API.  
- Всё через очереди (Redis), на каждый заказ — отдельный Job.  
- Sentry ловит ошибки, если маркетплейс отвечает 5xx или меняет схему.  

### Симптомы
Внезапно на проде **начали падать Job-ы обновления остатков** с ошибкой:  

```
SQLSTATE[40P01]: Deadlock detected: 
Deadlock found when trying to get lock; try restarting transaction
```

Ошибка возникала **нестабильно** — раз в 2–3 дня, всегда ночью, когда одновременно:
- запускался еженощный кросс-синхронизации с Ozon/WB (сотни Job-ов);
- два‑три менеджера одновременно генерировали **отчёт по Честному знаку**.

В логах Sentry — чёткая картина: транзакция 1 (Job обновления) и транзакция 2 (отчёт) блокируют друг друга.

### Почему не воспроизводилось локально
1. **Размер данных** — на проде в таблице `order_items` > 5 млн строк, в `honest_sign_codes` > 2 млн.  
   На локали — десятки тысяч, планировщик запросов выбирал **Index Scan**.  
   На проде — из‑за устаревшей статистики (или объёма) выбирался **Seq Scan** по всей таблице `honest_sign_codes`, который захватывал блокировки на гораздо большем диапазоне строк.  

2. **Конкуренция** — локально никто не гонял одновременно 200 Job и 2 Excel‑отчёта.  
   Даже если запустить пару потоков, deadlock не ловился, потому что план запроса был другим.  

3. **Уровень изоляции** — в production случайно стоял `REPEATABLE READ` (наследие старой миграции), что увеличивало зону блокировок при чтении. На локали использовался стандартный `READ COMMITTED`.

### Расследование (с помощью Sentry и логов)
1. Sentry показал два конфликтующих запроса.  
   - **Job‑обновление**: `UPDATE products SET stock = ... WHERE id = ?` (держит блокировку на конкретной строке products).  
   - **Отчёт**:  
     ```sql
     SELECT ...
     FROM honest_sign_codes hsc
     JOIN products p ON p.id = hsc.product_id
     LEFT JOIN order_items oi ON oi.product_id = p.id ...
     WHERE ...
     GROUP BY ...
     ```
     План запроса — `Seq Scan on honest_sign_codes`, затем `Nested Loop` с products и order_items.  
     При последовательном сканировании `honest_sign_codes` запрос захватывал **блокировки на все затронутые строки** таблиц, причём в порядке физического расположения, а не первичного ключа.

2. Job‑ы обновляли products в порядке, обрабатывая заказы с маркетплейсов. Обычно порядок: сначала апдейт продукта А, потом Б.  
   Отчёт же шёл по индексу `honest_sign_codes.product_id` и пытался прочитать продукт Б раньше А, создавая **циклическую зависимость блокировок**.

3. В итоге:  
   - Job захватил блокировку на продукт **A**, ждёт продукт **B**.  
   - Отчёт захватил блокировку на продукт **B**, ждёт продукт **A**.  
   - Deadlock.

### Решение
1. **Добавили составной индекс**  
   ```sql
   CREATE INDEX CONCURRENTLY honest_sign_codes_product_id_created_at 
   ON honest_sign_codes(product_id, created_at);
   ```
   Это позволило запросу отчёта использовать **Index Only Scan** и не захватывать блокировки на всю таблицу.

2. **Понизили уровень изоляции** для отчёта до `READ COMMITTED`.  
   В коде явно: `DB::transactionLevel() === 'REPEATABLE READ' ? 'READ COMMITTED' : ...`.

3. **Разнесли по времени** тяжёлые отчёты и массовые обновления — перенесли генерацию отчёта на отдельный выделенный реплику (read-only) через `->readOnly()` в Laravel.

4. **Deadlock retry** — добавили в Job автоматические повторные попытки при `40P01`.

После деплоя deadlock-и исчезли.

---

## 🔗 Как это связано с твоим опытом

✅ **Честный знак / Excel** — именно тот самый отчёт, таблицы PostgreSQL, выгрузка.  
✅ **Ozon/WB API + очереди** — фоновые Job-ы, которые создавали конкурентные апдейты.  
✅ **Sentry** — помог увидеть два конфликтующих запроса и план выполнения.  
✅ **Оптимизация запросов** — добавление индекса и изменение плана запроса.  
✅ **Инфраструктура** — ночная нагрузка, разделение чтения/записи.  
✅ **«Не воспроизводилось локально»** — из‑за разного объёма данных и статистики планировщика.  
✅ **Неочевидность** — никто не думал, что `SELECT` может вызывать deadlock, тем более в REPEATABLE READ.

---

Эта ситуация — классика e-commerce, когда **OLTP‑нагрузка (заказы, маркетплейсы)** встречается с **OLAP‑подобными отчётами** в одной транзакционной базе. Без хорошего мониторинга и понимания блокировок такой баг может жить годами. У тебя как раз был весь стек, чтобы его найти и исправить.