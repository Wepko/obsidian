## 🧩 Баг №4: «Невидимая миграция API, которая сломала кэш»

### Контекст

В legacy-проекте была старая админка на PHP (Twig + jQuery). Решили переписать её на React.  
Ты занимаешься **переносом API** — выносишь логику в отдельные REST-эндпоинты, которые будет потреблять новенький SPA.

Одна из ключевых страниц — **список заказов с пагинацией и фильтрацией**.  
Раньше старый контроллер возвращал HTML-представление, а сейчас мы делаем полноценный JSON API: `/api/v2/orders`.

**Задача:** сделать быстрый, отзывчивый интерфейс.  
Ты проводишь **оптимизацию запросов**: добавляешь индексы, жадишь eager loading, сокращаешь выборку полей.  
Также внедряешь **кэширование** ответов с тегированием (Redis): при обновлении заказа сбрасывается кэш списка.

Всё проходит код-ревью, тесты зелёные, выкатываем на прод.
### Симптомы

Через день после деплоя:
- Операторы колл-центра жалуются: **«Старая админка показывает неверные суммы заказов!»**
- А новая React-админка (на том же API) — показывает всё правильно.
- Ошибка **плавающая**: у одного заказа сумма верная, у другого — занижена в 2 раза, у третьего — вообще 0.
- Никаких эксепшенов, Sentry молчит, логи 200 OK.

### Локальное воспроизведение

На стейджинге всё ок.  
Старая админка (ещё используется параллельно при миграции) тоже обращается к **новому API**?  
Нет, старая админка всё ещё живёт на старых контроллерах, которые напрямую работают с БД.

Так откуда у неё берутся неверные суммы?

---

## 🕵️ Расследование

### Шаг 1. Выясняем источник данных для старой админки

Смотрим код старого контроллера `OrdersController`.  
Он не использует API, он делает запросы в БД сам.  
Но в шаблоне есть странный момент: раньше там выводилась сумма заказа напрямую из `$order->total`, а теперь появился кусок Twig:

twig

{% set orderData = fetch_api('/api/v2/orders/' ~ order.id) %}
Сумма: {{ orderData.total }}

Кто-то решил «унифицировать» отображение и подтягивать данные для старой админки из того же нового API, чтобы данные были консистентны.  
Этот код был добавлен **три месяца назад** другим разработчиком, но до сих пор работал корректно.

### Шаг 2. Анализируем изменения в API

Смотрим, что изменилось в эндпоинте `/api/v2/orders/{id}` в рамках рефакторинга.

**Было (до рефакторинга):**

php

public function show($id)
{
    $order = Order::with('items')->findOrFail($id);
    return response()->json([
        'id' => $order->id,
        'total' => (float) $order->total,   // 1234.56
        'items' => ...
    ]);
}

**Стало (после рефакторинга):**

php

public function show($id)
{
    $order = Order::with('items')->findOrFail($id);
    return response()->json([
        'id' => $order->id,
        'total' => (int) $order->total,   // 1234.56 → 1234 (потеря копеек!)
        'items' => ...
    ]);
}

**Причина:**  
При рефакторинге ты унифицировал типы полей во всех эндпоинтах.  
В БД поле `total` типа `DECIMAL(10,2)`.  
В старой админке оно всегда выводилось как число с двумя знаками.  
В новом API ты решил, что сумма должна быть целым числом в копейках (как хранят в некоторых системах), и преобразовал в `(int)`.  
Но забыл, что это изменение затронет **старую админку**, которая всё ещё пользуется этим API.

### Шаг 3. Почему проблема плавающая?

В старой админке кэширование ответов API не использовалось — каждый раз прямой запрос.  
Но **Redis-кэш**, который ты поставил для нового API, закэшировал ответ с `total` в виде целого числа.  
И этот кэш с неверным форматом иногда подхватывался старым кодом (если запрос попадал в то же время, когда кэш был актуален).

**Почему локально не воспроизводилось?**

- На локали кэш Redis был отключён или настроен на другое пространство имён.
    
- Старая админка на локали использовала мок-данные, а не реальный API.
    
- В тестах мы проверяли только новый API, но не проверяли, как старый фронтенд потребляет эти данные.
    

---

## 🧠 Решение

### 1. Откатываем изменение типа

Возвращаем `(float)` или `(string) number_format($order->total, 2)`.  
В идеале — явно форматировать сумму в JSON как строку с двумя знаками, либо оставлять float, но документировать формат.

### 2. Инвалидируем кэш

Очищаем весь кэш для эндпоинтов заказов, чтобы убрать кривые значения.

### 3. Версионирование API

Создаём новую версию `/api/v3/orders`, где уже корректный формат, и переключаем новую админку на v3.  
Старая админка остаётся на v2 (с восстановленным форматом).  
Это позволяет безопасно рефакторить API, не ломая старых клиентов.

### 4. Добавляем тесты на обратную совместимость

В CI/CD прогоняем тесты, которые имитируют запросы от старой админки (заранее записанные примеры ответов).  
При изменении формата поля — падаем.

### 5. Мониторинг изменений структуры ответа

Внедряем в staging-окружении проверку: сравниваем ответы API до и после деплоя.  
Если ключ изменил тип или пропал — алерт.

---

## 💥 Итог

- Проблема решена за час.
    
- Старая админка снова показывает верные суммы.
    
- Команда вводит политику: **любое изменение API должно проходить через запрос с обоснованием обратной совместимости**.
    
- В Sentry добавляем проверку на несоответствие типов в ответах (например, если total вдруг стал целым, а ожидается float).
    

---

## 🔗 Как это связано с твоим опытом

✅ **Перенос API со старой админки в React-админку** — центральная роль.  
✅ **Оптимизация запросов и рефакторинг** — мы «унифицировали» типы, но сломали совместимость.  
✅ **Поддержка инфраструктуры** — Redis-кэш усугубил проблему, сделав её плавающей.  
✅ **Неочевидность** — баг не в новом коде, а в том, как старый код использует новый API. Никто не ожидал, что изменение типа в ответе повлияет на старую Twig-админку.  
✅ **Не воспроизводится локально** — потому что старая админка на локали не была настроена на реальное API, а тесты покрывали только изолированные компоненты.

Этот случай — отличная иллюстрация того, как **рефакторинг бэкенда ради нового фронтенда** может сломать старые, забытые интеграции. Очень жизненная ситуация в e‑commerce, где миграция на React-админку часто идёт параллельно с работой legacy-интерфейса.