Вот третий пример, завязанный на **демо-версию**, **умный поиск**, **сидеры** и **конфигурацию окружений**. Этот баг был особенно коварным — он не падал, не сыпал ошибками, но **тихо подменял данные** в продакшене.

---

## 🔎 Баг №3: «Призрачные товары из песочницы»

### Контекст
У вас в e‑commerce есть **умный поиск** на базе Elasticsearch.  
Каждый час крутится задача: выгружать товары из PostgreSQL → индексировать в ES.  
Для этого используется отдельный индекс `prod_products`.

Также есть **демо-версия сайта** — полная копия продакшена, но с тестовыми данными.  
Ты написал сложный механизм заполнения: сидеры + фабрики, которые генерируют тысячи вымышленных товаров, заказов, пользователей.  
Для демо-версии используется свой Elasticsearch-индекс: `demo_products`.

Демо-версия запущена на том же кластере ES, что и прод, но с разными именами индексов.  
Всё работает изолированно.

### Симптомы
Через несколько недель после запуска демо-портала отдел маркетинга замечает странности:

- В поиске на **продакшене** иногда показываются товары с **ценою 0 рублей** или **названием типа «Test Product #42»**.
- Эти товары нельзя купить — при переходе 404.
- Проблема появляется **нестабильно**, чаще всего днём, в часы пик.
- В логах Elasticsearch — ничего, Sentry молчит.

### Локальное воспроизведение
На локали всё чисто:
- Продовый индекс содержит только реальные товары.
- Демо-индекс — только тестовые.
- Никакого пересечения.

---

## 🕵️ Расследование

### Шаг 1. Откуда тестовые товары в проде?
Смотрим индексы ES через Kibana:
- `prod_products_v23` — правильный, 150k документов.
- `demo_products_latest` — 50k тестовых товаров.
- Внезапно находим **старый индекс** `prod_products_v22`, который давно должен быть удалён.  
Внутри него — **смесь** реальных и тестовых товаров.

Оказывается, когда настраивали демо-версию, **алиас `prod_products`** указывал на `prod_products_v22`.  
При переключении на новый индекс `prod_products_v23` забыли убрать алиас со старого.  
И самое главное: **демо-версия по ошибке индексировала свои товары в старый индекс**, потому что в её конфиге было захардкожено имя индекса без учёта окружения.

### Шаг 2. Почему это происходило нестабильно?
В демо-версии был реализован **сложный метод заполнения данными** (Seeds + Fabric + Job dispatch).  
При создании нового демо-клиента система:
1. Копирует структуру БД.
2. Запускает сидеры, которые генерируют тестовые товары.
3. Отправляет **Job** в очередь на переиндексацию этих товаров в Elasticsearch.

**Проблема:** В Job передавалось имя индекса через `config('elasticsearch.index')`.  
А конфиг на продакшене был `'index' => 'prod_products'`.  
В демо-окружении этот конфиг должен переопределяться на `demo_products`, но **при деплое демо-версии забыли выставить переменную окружения**.  
В итоге демо-сервер читал продовый конфиг и писал тестовые товары в индекс, на который указывал алиас `prod_products` — а это был старый `prod_products_v22`.

### Шаг 3. Почему ошибка не падала в Sentry?
- Обращение к Elasticsearch было успешным: индекс существует, документы записываются.
- Старый индекс больше не использовался проде для чтения (кроме пары закэшированных запросов, которые ещё висели в старом коде — их и находил маркетинг).
- Sentry был настроен только на HTTP 5xx и исключения PHP, а тут исключений не было — просто запись не туда.

### Шаг 4. Как демо-триггер активировался?
Днём, в часы пик, запускалось **массовое создание демо-песочниц** (например, для менеджеров или потенциальных клиентов).  
Это вызывало поток Job-ов, которые лезли в Elasticsearch с неправильным индексом и засоряли старый индекс тысячами тестовых товаров.  
На локали такого не было, потому что локально конфиг был настроен верно (или использовался docker-композ с чистым ES).

---

## 🧹 Решение

### 1. Удаляем старый индекс
Снимаем алиас с `prod_products_v22`, удаляем индекс полностью.

### 2. Жёстко разделяем конфиги по окружениям
```php
// config/elasticsearch.php
return [
    'index' => env('ELASTICSEARCH_INDEX', 'prod_products'),
];
```
В `.env` демо-сервера прописываем:
```
ELASTICSEARCH_INDEX=demo_products
```

### 3. Добавляем защиту от случайной записи не в тот индекс
В классе индексации добавляем проверку окружения:
```php
if (app()->environment('production') && !str_starts_with($indexName, 'prod_')) {
    throw new \RuntimeException("Attempt to write to non-prod index on production!");
}
```

### 4. Логируем все записи в ES
Добавляем `info()` с именем индекса и количеством документов — в демо-режиме пишем в отдельный канал логов, в проде — только warning при несовпадении паттерна.

### 5. Пересматриваем сидеры
Добавляем в демо-сидеры явный `Context::demo()` или проверку `App::environment('demo')`, чтобы случайно не запустить их на проде (хотя это уже другая история).

---

## 💥 Итог
После исправлений:
- Тестовые товары перестали появляться в проде.
- Маркетинг перестал жаловаться.
- Мы добавили автоматический аудит конфигурации при каждом деплое (CI проверяет, что на проде `ELASTICSEARCH_INDEX` начинается с `prod_`).
- В Sentry появилось предупреждение при попытке демо-сервера писать в продакшен-индекс (благодаря добавленному исключению).

---

## 🔗 Как это связано с твоим опытом

✅ **Демо-версия сайта** — ты сам реализовывал сложный метод заполнения тестовыми данными (Seeder, Fabric), диспатчил Job.  
✅ **Умный поиск** — интеграция с Elasticsearch, индексация.  
✅ **Инфраструктура и поддержка** — ошибка конфигурации окружений, скрытое влияние на прод.  
✅ **Неочевидность** — баг не падал, не выдавал ошибок, просто «засорял» старые данные. Локально не воспроизводился, потому что конфиги отличались.  
✅ **Воспроизводился под нагрузкой** — массовое создание демо-песочниц днём вызывало поток индексации.  

Этот баг — классика devops-ловушек: когда **демо-среда начинает думать, что она прод**, а прод страдает от призрачных товаров. Идеально ложится на твой опыт: ты занимался и демо-версией, и поиском, и интеграциями.